# Expo Public Variables (REQUIRED for mobile app)
# These variables must start with EXPO_PUBLIC_ to be accessible in the app
# Set these in your root .env file or via EAS secrets for builds

# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlvdXItcHJvamVjdC1pZCIsInJvbGUiOiJhbm9uIiwiaWF0IjoxNjQwOTk4ODAyLCJleHAiOjE5NTY1NzQ4MDJ9.example

# Backend API URL (tRPC endpoint)
# For local development: http://localhost:5000
# For production: https://api.yourdomain.com
EXPO_PUBLIC_API_URL=http://localhost:5000

# Security Note:
# - Only variables prefixed with EXPO_PUBLIC_ are accessible in the mobile app
# - Never expose SUPABASE_SERVICE_ROLE_KEY - it's server-side only
# - These values are bundled into the app, so use environment-specific values for production builds
#
# For EAS builds, set these as secrets:
#   eas secret:create --scope project --name EXPO_PUBLIC_SUPABASE_URL --value https://your-project.supabase.co
#   eas secret:create --scope project --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value your-anon-key
#   eas secret:create --scope project --name EXPO_PUBLIC_API_URL --value https://api.yourdomain.com

