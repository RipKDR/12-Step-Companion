# Server Configuration
PORT=5000
NODE_ENV=development

# Google Gemini API Key for AI Sponsor Chat Feature
# Get your API key from: https://makersuite.google.com/app/apikey
# Free tier: 60 requests/minute, 1,500 requests/day
# Optional: Leave empty if you don't need AI Sponsor feature
GEMINI_API_KEY=

# Database Configuration (PostgreSQL)
# Required if using cloud sync or authentication
# Get a free database from: https://neon.tech
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=

# Session Secret (Required for authentication)
# Generate a random secret: openssl rand -base64 32
# Optional: Leave empty to run in local-only mode (no auth)
SESSION_SECRET=

# Replit Auth Configuration (Optional)
# Only required if deploying to Replit or using Replit Auth
# Leave empty for local development or other platforms
REPL_ID=
ISSUER_URL=https://replit.com/oidc
