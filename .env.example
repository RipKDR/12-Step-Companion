# Environment Variables Template for Replit Deployment
# Copy these to Replit Secrets before deploying

# CRITICAL: Session secret for authentication (REQUIRED)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-64-character-hex-string-here

# Database connection (auto-provisioned by Replit)
# This should be automatically set by Replit when PostgreSQL module is enabled
DATABASE_URL=postgresql://user:password@host:port/database

# Replit-provided variables (auto-set, no action needed)
REPL_ID=automatically-set-by-replit
ISSUER_URL=https://replit.com/oidc
NODE_ENV=production
PORT=5000
